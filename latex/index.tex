\chapter{Télécom Paris INF224 Project}
\hypertarget{index}{}\label{index}\index{Télécom Paris INF224 Project@{Télécom Paris INF224 Project}}
\label{index_md__r_e_a_d_m_e}%
\Hypertarget{index_md__r_e_a_d_m_e}%
 \hypertarget{index_autotoc_md1}{}\doxysection{\texorpdfstring{C++ Part}{C++ Part}}\label{index_autotoc_md1}
\hypertarget{index_autotoc_md2}{}\doxysubsection{\texorpdfstring{Q4}{Q4}}\label{index_autotoc_md2}
Pure virtual function. We put {\ttfamily virtual} in the front and {\ttfamily =0} in the end of the definition. We cannot create objects of this class because this function is not well defined. We should create a derived class and override this function in order to have it defined.\hypertarget{index_autotoc_md3}{}\doxysubsection{\texorpdfstring{Q5}{Q5}}\label{index_autotoc_md3}
Polymorphism. In C++, we need virtual methods and virtual destructor. The pointers. In Java, there is no problem because the objects are passed by references.\hypertarget{index_autotoc_md4}{}\doxysubsection{\texorpdfstring{Q7}{Q7}}\label{index_autotoc_md4}
If we do the shallow copy, these pointers simply point to the same object. In this case, if we delete one pointer, the pointed object is destructed according to the destructor. Thus, the other pointer point to an address which contains unknown data. To solve this problem, we need to overload the operator {\ttfamily =}. We should do the deep copy. That is to say, when copying pointers, we create a new copy to the pointed object and make the pointer point to it.\hypertarget{index_autotoc_md5}{}\doxysubsection{\texorpdfstring{Q8}{Q8}}\label{index_autotoc_md5}
It should be a list of pointers because they need to point to objects of different multimedia types. That\textquotesingle{}s how polymorphism in C++ works.\hypertarget{index_autotoc_md6}{}\doxysubsection{\texorpdfstring{Q10}{Q10}}\label{index_autotoc_md6}
To prevent creating the object with {\ttfamily new}, we can set the constructor to {\ttfamily private}.\hypertarget{index_autotoc_md7}{}\doxysubsection{\texorpdfstring{Q12, Q13 not treated}{Q12, Q13 not treated}}\label{index_autotoc_md7}
\hypertarget{index_autotoc_md8}{}\doxysection{\texorpdfstring{Java Swing Part}{Java Swing Part}}\label{index_autotoc_md8}
\hypertarget{index_autotoc_md9}{}\doxysubsection{\texorpdfstring{Q1}{Q1}}\label{index_autotoc_md9}
We notice that after many clicks, the text exceeds the window. We can use the {\ttfamily JScroll\+Pane} component that adds a vertical and horizontal scrollbar when necessary. 